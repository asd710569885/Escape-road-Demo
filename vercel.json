{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd Escape-road && npm install && vite build",
  "outputDirectory": "Escape-road/dist",
  "framework": "vite"
}