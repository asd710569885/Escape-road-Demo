{
  "version": 2,
  "builds": [
    {
      "src": "Escape-road/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "cd Escape-road && npm install && npm run build",
        "outputDirectory": "Escape-road/dist",
        "distDir": "Escape-road/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/Escape-road/dist/index.html"
    }
  ]
}