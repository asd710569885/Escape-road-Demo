{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd Escape-road && npm install --production=false && npm run vercel-build",
  "outputDirectory": "Escape-road/dist",
  "framework": "vite"
}